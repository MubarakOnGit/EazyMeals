import 'package:eazy_meals/screens/VerificationScreen.dart';
import 'package:eazy_meals/screens/splash_screen.dart';
import 'package:flutter/material.dart';
import 'package:firebase_core/firebase_core.dart';
import 'package:firebase_auth/firebase_auth.dart';
import 'firebase_options.dart'; // Generated by FlutterFire CLI
import 'package:eazy_meals/screens/onboarding_screen.dart'; // Adjust path as needed
import 'package:eazy_meals/screens/login_screen.dart'; // Adjust path as needed// Add this import

import 'home/customer_dashboard.dart';
import 'home/home_screen.dart'; // Adjust path as needed

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: SplashScreen(), // Use SplashScreen as the entry point
      routes: {
        '/onboarding': (context) => OnboardingScreen(),
        '/login': (context) => LoginScreen(),
        '/CustomerDashboard': (context) => CustomerDashboard(),
        '/verification': (context) => VerificationScreen(), // Add this route
      },
    );
  }
}
